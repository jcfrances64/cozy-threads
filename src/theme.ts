import { createTheme } from '@mui/material/styles';

export const appTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: '#fcfaf2',
    },
    secondary: {
      main: '#4860b8',
    },
  },
});

